apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  push:
    branches:
      - "**"
jobs:
  build:
    steps:
      - name: SonarQube
        with:
          language: JAVA
        uses: cloudbees-io/sonarqube-bundled-sast-scan-code@v2
